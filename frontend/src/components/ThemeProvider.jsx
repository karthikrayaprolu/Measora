// Dark mode removed — ThemeProvider is no longer used.
// This file is kept to avoid breaking any stale imports during development.
export function ThemeProvider({ children }) {
  return children;
}
